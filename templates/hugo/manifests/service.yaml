# Service fronting the hugo-site Deployment.
---
apiVersion: v1
kind: Service
metadata:
  name: hugo-site
  namespace: default
spec:
  selector:
    app: hugo-site
  ports:
    - name: http
      port: 80
      targetPort: 80
