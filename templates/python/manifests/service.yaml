# Cluster-internal Service fronting the pyapp Deployment.
---
apiVersion: v1
kind: Service
metadata:
  name: pyapp
  namespace: default
spec:
  selector:
    app: pyapp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
