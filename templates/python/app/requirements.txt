# Local-dev deps (drives `uv pip install -r app/requirements.txt` into the
# repo-root .venv that .envrc activates). The container IMAGE does NOT read
# this file — it pulls deps from nixpkgs via flake.nix → packages.image
# (python3.withPackages). Keep the two in rough parity.
#
# variant = "fastapi":
fastapi
uvicorn
#
# variant = "script": replace the two lines above with whatever your script
# imports (the scaffolded script.py uses only the stdlib, so none are needed).
